// Derived from Vec with four elements
// Widely used in OpenCV to pass and read pixel values.

// To access Vec and Scalar valeus, we use the [] operator.

Scalar s0(0);
Scalar s1(0.0, 1.0, 2.0, 3.0);
Scalar s2(s1);